//
//  ProfilePictures.swift
//  AmazCopy
//
//  Created by Christian Luiz Moraes Braga on 08/07/24.
//

import SwiftUI

struct ProfilePictures: View {
     
    //@State var pictureNew: String
    
    var body: some View {
      /*
        NavigationLink {
            ProfilePicturesView(changePic: $pictureNew, pictures: profilePicturesMock[0])
        } label: {
            VStack {
                Image(pictureNew)
                    .resizable()
                    .scaledToFit()
                    .frame(width: 80)
                
                Text("Alterar")
                    .foregroundStyle(.gray)
            }
        }
        .padding(.bottom)*/
        VStack{}
    }
}

#Preview {
    ProfilePictures()
}
